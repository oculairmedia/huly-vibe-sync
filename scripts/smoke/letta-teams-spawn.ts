#!/usr/bin/env bun
/**
 * Runtime-path smoke entrypoint kept for the vibesync-6wn regression guard.
 * The implementation remains in letta-teams-code-review.ts for backwards
 * compatibility with existing runbooks; this file is the new explicit name
 * used by the formula-run smoke split.
 */
import './letta-teams-code-review.ts';
